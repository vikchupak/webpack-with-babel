export const listItems = [];// Array of DOM elements for init render
export const gap       = 7;
export const sortables = []; // Array of sortables
export let total       = null;

export function setTotal(newTotal) {
    total = newTotal;
}